---
name: video-publisher
description: "从 topic 到竖屏短视频的一站式全流程 Skill。支持：根据 topic 全网搜索素材 → 事实核查 → 按模板写口播稿 → 自动拆成分镜稿 → 下载 B-roll → AI 配音 → Remotion 渲染出片。用户可选择：仅出稿（人工确认后出片）或 全自动出片。触发词：写一期视频、生成一条XX的视频、出一期XX的口播稿并渲染出片、帮我做一期XX案例的视频。"
license: UNLICENSED
agent_created: true
disable: false
---

# Video Publisher — 从 topic 到竖屏短视频，一站全流程

## 一句话职责

用户输入一个 **topic**（主题/选题），指定（或被询问后补充）**内容类型**，Skill 自动完成：全网搜索素材 → 事实核查 → 按模板写口播稿 → 自动拆分成镜稿 → 下载 B-roll 素材 → AI 配音 → Remotion 渲染 mp4 + 抽封面。**用户可选仅出稿待确认，或全自动出片。**

---

## 何时使用本技能

- 用户说"写一期XX的视频/口播稿/案例"
- 用户说"帮我生成一条XX主题的短视频"
- 用户说"出一期XX，自动出片"
- 用户说"写一篇XX的口播稿，然后渲染成视频"
- 用户给出一个 topic，要求从写稿到出片全流程
- 用户说"每天自动出一期视频"、"定时任务"、"每天跑一期"

---

## 工作流总览

```
用户输入 topic [+ 内容类型] [+ 自动出片?]
    │
    ▼
┌─ 前置步骤：全局去重保障（所有触发方式共用）──┐
│  (0) 读取 scripts/.publish-history.json        │
│  (0) 对 topic 执行去重判断（见下方章节）        │
│  (0) 判定重复 → 拒绝/调整/换题                │
│  (0) 判定不重复 → 继续                        │
└──────────────────────────────────────────────┘
    │
    ▼
┌─ 阶段 1：写稿（零依赖，装 Skill 即用）──────┐
│  (1) 确定内容类型（若未指定则询问）          │
│  (2) 研究：全网搜索素材                      │
│  (3) 事实核查：交叉验证、信源标注            │
│  (4) 写作：按模板生成口播稿 .md              │
│  ┌─────────────────────────────────────┐    │
│  │ 用户说"自动出片" → 直接进入阶段 2    │    │
│  │ 用户说"写稿"或"出稿"→ 交付待确认    │    │
│  └─────────────────────────────────────┘    │
│  产出：scripts/<slug>-视频文稿.md            │
└──────────────────────────────────────────────┘
    │
    ▼ 用户确认后 / 或自动进入
    │
┌─ 阶段 2：出片（自动部署 Remotion 项目）─────┐
│  (5) 检测 git 环境（无则自动安装）            │
│  (6) git clone remotion 项目                  │
│  (7) npm run setup 自动部署                   │
│  (8) 口播稿 → content/<slug>.md 分镜稿        │
│  (9) 下载 B-roll 素材（Pexels）               │
│  (10) 生成 AI 配音 + 字幕时间轴（TTS）        │
│  (11) Remotion 渲染 mp4 + 抽封面              │
│  产出：out/<slug>/<slug>.mp4 + 封面          │
└──────────────────────────────────────────────┘
```

---

## 全局去重保障（所有触发方式通用）

**无论以何种方式触发**（用户手动输入 topic、自然语言描述需求、定时任务自动触发），**每次写稿前必须执行去重检查**，确保不产出雷同内容。

> ⚠️ 这是硬性规则，不可跳过。Agent 必须执行去重判断才能进入写稿阶段。

### 历史记录文件

**文件路径：** `scripts/.publish-history.json`

每次运行完成后，自动更新此文件，记录以下信息：

```json
{
  "version": 1,
  "lastRun": "2026-09-04T08:00:00+08:00",
  "entries": [
    {
      "slug": "pre-wedding-anxiety",
      "topic": "婚前焦虑深度拆解",
      "type": "案例型",
      "enterprise": "",
      "keywords": ["婚前焦虑", "恐婚", "心理学"],
      "date": "2026-09-04",
      "output": "out/pre-wedding-anxiety/pre-wedding-anxiety.mp4"
    },
    {
      "slug": "dating-communication",
      "topic": "相亲中不会聊天的底层逻辑",
      "type": "教程型",
      "enterprise": "",
      "keywords": ["相亲沟通", "聊天技巧", "社交技能"],
      "date": "2026-09-03",
      "output": "out/dating-communication/dating-communication.mp4"
    }
  ]
}
```

**文件职责：**
- 记录过去所有已生成的内容（topic + 类型 + 关键词 + 企业/赛道）
- 作为去重判断的依据
- 用户可手动查看/编辑此文件，删除某个条目即可"允许重新生成该主题"
- 当文件不存在时（首次运行），Agent 自动创建并跳过去重判断

### 去重/相似度判断规则

**触发时机：** 每次写稿前，无论用户手动输入还是定时任务自动触发。

| 判断维度 | 比对方式 | 判定为"重复"的条件 |
|---|---|---|
| **完全匹配** | topic 字符串完全一致 | 直接判定重复 |
| **关键词重叠** | 新 topic 的 keywords 与历史记录中任一 entry 的 keywords | 重叠 ≥2 个关键词 → 判定重复 |
| **企业/实体重叠** | 案例型：检查 enterprise 字段 | 同一企业 → 判定重复（除非确认是新角度） |
| **语义相似度** | 将新 topic 与历史记录 topic 做语义评估 | 核心含义相同（如"婚前焦虑"和"恐婚的根源"）→ 判定重复 |
| **角度雷同** | 同一关键词但切入角度相同 | "教你怎么聊天"和"学会聊天" → 判定重复 |

#### 案例型专属去重（更严格）

案例型涉及具体企业/技术/人物，去重要求更高：

```
同一企业/技术 → 至少间隔 30 天才能再次生成（除非该案例有重大新进展）
同一赛道（如"心理咨询"）→ 至少间隔 7 天
同一角度（如"焦虑"）→ 至少间隔 14 天
```

### 处理方式

```
判定为"不重复" → 直接进入写稿流程

判定为"重复" → ① 如果是定时任务自动触发 → 从储备池选下一个选题
                ② 如果是用户手动输入 → 告知用户该 topic 已有雷同内容，
                   建议调整角度或换题，并提供差异化建议
                ③ 如果用户坚持用同一 topic → 要求用户说明新角度/新方向，
                   确认与历史记录有足够差异后才放行

定时任务储备池全部判定重复 → 启动自动补充机制
```

### 运行后更新历史

每次写稿 + 出片完成后（无论是否渲染），自动将本次内容追加到 `.publish-history.json`，确保下次运行有完整的历史记录可供比对。

---

## 内容类型系统

### 用户调用时，必须明确内容类型

如果用户没有指明类型，**主动询问**，提供以下选项：

| 类型 | 适用场景 | 结构特点 |
|---|---|---|
| **案例型** | 企业/技术/产品案例分析 | 三钩子开头（悬念→对比→主题）+ 六段式主体 |
| **科普型** | 知识讲解、概念解读 | 悬念式开头（现象→问题→预告）+ 四段式主体 |
| **观点型** | 评论、解读、分析 | 观点前置开头（结论→共鸣→预告）+ 三段式论据 |
| **教程型** | How-to、操作指南、步骤 | 问题引入开头（痛点→承诺→预告）+ 分步式主体 |
| **其他** | 用户自定义 | 自由适配，默认三段式（引入→核心→收束） |

### 自动判定规则

如果用户说"案例"、"企业"、"案例速览"、"EPxxx" → 自动判定为案例型

---

## 阶段 1：写稿（零依赖）

### 第 1 步：确定内容类型

- 如果用户已指明类型 → 直接使用
- 如果未指明 → 主动询问，展示选项
- 关键词自动判定（见上方规则）

### 第 2 步：确定输出目录

- 优先使用用户指定的路径
- 未指定时，询问用户希望存到哪里
- 若用户说"默认"或"你看着放" → 存到当前对话工作目录下的 `scripts/` 目录
- **输出文件命名规则**：`<slug>-视频文稿.md`，slug 由 topic 英文/拼音生成

### 第 3 步：研究阶段

**案例型**（研究框架）：
- 搜索该案例的核心企业/技术/产品信息
- 搜索近期官媒报道（新华社、央视、人民日报、行业媒体）
- 收集关键数据（效率/安全/经济/绿色四类）
- 搜索对比数据（旧做法 vs 新方案）
- 搜索可验证的独立信源（≥2 个转化为采用）

**科普型**：
- 搜索该概念的核心定义、原理、发展历程
- 搜索通俗易懂的类比/比喻
- 搜索相关数据支撑

**观点型**：
- 搜索相关事实、数据、案例作为论据
- 搜索不同立场的观点做对比

**教程型**：
- 搜索步骤/方法/最佳实践
- 搜索常见错误/注意事项

### 第 4 步：事实核查阶段

- 所有量化数据必须 ≥2 个独立信源（同源通稿不算两个）
- 仅单一信源的数据标注「单一信源/待核实」
- 仅见商业平台转载的数据不入解说
- 企业名称、技术名词、发布时间准确无误
- 无数量级矛盾（如「提升 200%」与「提升 2 倍」混用）
- 涉及地图必须使用标准底图、国界线完整

### 第 5 步：写作阶段

根据内容类型选择对应的模板。**详细模板见 `references/script-template.md`**。

#### 字数控制总表

| 类型 | 总字数目标 | 语速 | 对应时长 |
|---|---|---|---|
| 案例型 | 1150–1200 字 | 4.0 字/秒 | ~5 分钟 |
| 科普型 | 1000–1200 字 | 4.0 字/秒 | 4–5 分钟 |
| 观点型 | 900–1100 字 | 4.0 字/秒 | 4–4.5 分钟 |
| 教程型 | 900–1100 字 | 4.0 字/秒 | 4–4.5 分钟 |
| 其他 | 800–1200 字 | 4.0 字/秒 | 按实际 |

#### 案例型 — 三钩子写作框架（硬性规则）

```
Hook ① 提问/反转钩子
  "你知道吗，……？"
  用一句反常识/新信息/悬念勾起好奇心。
  字数：~30 字

Hook ② 对比冲击
  "当你还在「旧的费时做法」时，……已经帮你的同行做到「震撼结果」了。"
  制造落差：痛点可视化 + 主角价值。
  字数：~40 字

Hook ③ 确立主题 + 与观众的相关性
  "这就是今天要讲的 XX 案例——……如何帮……破局。"
  点名主题 + 告诉观众"与我有关"。
  字数：~30 字
```

#### 案例型完整结构

```
## 1. 案例卡片
行业赛道｜企业｜核心技术｜关键成效｜可复制指数

## 2. 分段解说词 + 分镜表
### 开头三钩子
| ① 提问/反转钩子 | ~30字 | | 素材检索词 |
| ② 对比冲击      | ~40字 | | 素材检索词 |
| ③ 确立主题+相关性 | ~30字 | | 素材检索词 |

### 六段式主体
| 段 | 段落名 | 字数 | 解说词 | 画面来源/素材检索词 |
| ① | 标题与一句话价值 | ~30 | | |
| ② | 转型背景与痛点 | ~150 | | |
| ③ | 智能化方案与技术路径 | ~350 | | |
| ④ | 实施过程与关键决策 | ~250 | | |
| ⑤ | 量化成效 | ~250 | | |
| ⑥ | 可复制经验与启示 | ~170 | | |

## 3. 配音与字幕规范
## 4. 音乐音效总表
## 5. 官媒信源清单
## 6. 事实核查与风险提示
## 7. 时长调节阀
## 8. 后续选题储备池
## 9. 备注
```

#### 科普型结构

```
## 1. 分段解说词 + 分镜表
### 开头（悬念式）
| ① 现象钩子 | ~30字 | "你有没有发现……？" | 素材检索词 |
| ② 问题钩子 | ~30字 | "但你可能不知道，……" | 素材检索词 |
| ③ 预告     | ~20字 | "今天我们就来拆解一下……" | 素材检索词 |

### 四段式主体
| 段 | 段落名 | 字数 | 解说词 | 素材检索词 |
| ① | 概念定义/本质 | ~200 | 通俗语言解释核心概念 | |
| ② | 拆解分析 | ~400 | 核心段，拆成几层+每层类比 | |
| ③ | 影响/意义 | ~250 | 这个概念为什么重要 | |
| ④ | 总结/思考 | ~200 | 收束+引发思考 | |

## 2. 配音与字幕规范
## 3. 音乐音效总表
## 4. 信源清单
## 5. 时长调节阀
## 6. 备注
```

#### 观点型结构

```
## 1. 分段解说词 + 分镜表
### 开头（观点前置）
| ① 结论先行 | ~20字 | "我今天想聊一个观点：……" | 素材检索词 |
| ② 共鸣/争议 | ~40字 | "你可能觉得……，但……" | 素材检索词 |
| ③ 展开预告 | ~20字 | "我从 X 个方面来拆。" | 素材检索词 |

### 三段式主体
| 段 | 段落名 | 字数 | 解说词 | 素材检索词 |
| ① | 论据一 | ~250 | 事实/数据/案例支撑 | |
| ② | 论据二 | ~250 | 递进或对比 | |
| ③ | 论据三+升华 | ~300 | 提到更高视角 | |

### 结尾
| 重申观点+呼吁 | ~100字 | | |

## 2. 配音与字幕规范
## 3. 音乐音效总表
## 4. 信源/个人观点标注
## 5. 时长调节阀
## 6. 备注
```

#### 教程型结构

```
## 1. 分段解说词 + 分镜表
### 开头（问题引入）
| ① 痛点钩子 | ~30字 | "你是不是也遇到过……？" | 素材检索词 |
| ② 结果承诺 | ~30字 | "今天教你一个方法，XX分钟搞定。" | 素材检索词 |
| ③ 步骤预告 | ~20字 | "一共分 X 步。" | 素材检索词 |

### 分步式主体
| 段 | 段落名 | 字数 | 解说词 | 素材检索词 |
| ① | 第一步 + 关键动作 | ~250 | 具体操作+原因 | |
| ② | 第二步 + 关键动作 | ~250 | 递进 | |
| ③ | 第三步 + 关键动作 | ~250 | 收束 | |
| ④ | 避坑/要点总结 | ~150 | 常见错误 | |

### 结尾
| 鼓励+行动号召 | ~100字 | | |

## 2. 配音与字幕规范
## 3. 音乐音效总表
## 4. 信源/经验说明
## 5. 时长调节阀
## 6. 备注
```

#### 其他类型（用户自定义）

按用户描述的结构灵活适配。若用户描述不清，主动给出 2-3 个结构选项供选择：

```
## 1. 分段解说词 + 分镜表
### 开头（悬念式/共鸣式/观点式 三选一）
### 主体（默认三段式：引入→核心内容→收束）
### 结尾

## 2. 配音与字幕规范
## 3. 音乐音效总表
## 4. 信源（如有）
## 5. 时长调节阀
## 6. 备注
```

### 第 6 步：交付与分支判断

**关键规则：根据用户输入决定是否继续出片。**

```
用户说"自动出片"、"生成一条XX的视频"、"渲染出片"、"自动出视频"
  → 标记为"自动模式"，交付口播稿后直接进入阶段 2（无需用户再次确认）

用户说"写一期XX的口播稿"、"出稿"、"帮我写篇稿子"
  → 交付口播稿，标记"待确认"，告知用户确认后告知即可自动出片

用户说"写稿并出片"、"写一期XX，自动出视频"
  → 标记为"自动模式"，交付口播稿后直接进入阶段 2
```

- 将完整的口播稿写入输出目录（默认 `./scripts/<slug>-视频文稿.md`）
- 告知用户文件路径和总字数
- 自动模式：告知用户"即将进入渲染阶段"
- 待确认模式：告知用户"确认后回复我，即可自动出片"

---

## 阶段 2：出片（自动部署 Remotion 项目）

### 第 5 步：检测并安装 git（自动）

进入阶段 2 时，首先检查系统是否已安装 git：

**检测方法：** 尝试执行 `git --version`，如果返回错误说明未安装。

**各平台自动安装 git 指引：**

| 平台 | 安装命令 | 说明 |
|---|---|---|
| macOS | `xcode-select --install` | 会弹出安装弹窗，用户确认后自动安装 Command Line Tools（含 git） |
| Windows | `winget install Git.Git` 或从 https://git-scm.com 下载 | winget 是 Windows 内置包管理器，无需额外安装 |
| Linux (Debian/Ubuntu) | `sudo apt install git -y` | |
| Linux (CentOS/RHEL) | `sudo yum install git -y` | |
| Linux (Arch) | `sudo pacman -S git --noconfirm` | |

> **安装后验证：** 再次执行 `git --version`，确认安装成功后才继续下一步。
> **注意：** 安装 git 可能需要用户确认密码（macOS 弹窗 / Linux sudo），Agent 需要引导用户完成操作。

### 第 6 步：git clone remotion 项目（自动）

git 就绪后，自动将 remotion 渲染引擎克隆到本地：

```bash
# 克隆到用户指定的目录，或默认克隆到当前工作目录
git clone https://github.com/scarecrowang/remotion-video-publisher.git
cd remotion-video-publisher
```

**注意：** 如果目标目录已存在（说明之前克隆过），则跳过 clone，直接进入该目录执行 `git pull` 拉取最新代码。

### 第 7 步：自动部署（npm run setup）

进入 remotion 项目目录后，自动执行：

```bash
npm run setup
```

setup 脚本会自动完成：
- `npm install` 安装 Node.js 依赖
- 检测 ffmpeg（可选，缺失时降级估算）
- 检测/下载 Chromium（渲染引擎）
- 创建 `.env` 模板文件（如果不存在）

> **如果 npm run setup 执行失败：** 检查是否已安装 Node.js（`node -v`），如果未安装，提示用户从 https://nodejs.org 下载安装 Node.js 18+ 后重试。

### 第 8 步：口播稿 → 分镜稿

将阶段 1 产出的口播稿，按以下规则映射为 `content/<slug>.md` 分镜稿：

**映射规则（参考 `references/md-grammar.md`）：**
- 每镜必含 `caption:`（口播本体 + 卡拉OK字幕），一句话 ≤ 40 字
- 超长的 caption 拆成连续两镜
- 数据段 → 用 `data`/`stats` 卡
- 概念/规律段 → 用 `theory` 卡
- 要点列表 → 用 `points` 卡
- 开头/结尾 → 用 `head`/`out` 卡
- `## meta` 里写 `theme:`（根据题材类型）和 `title:`
- 每镜配 `broll:` 英文关键词（素材检索线索）

**分镜稿存放位置：** remotion 项目的 `content/<slug>.md`

### 第 9 步：一键出片（后台运行，避免超时）

渲染视频是耗时操作（3-5 分钟口播稿通常需要 5-15 分钟渲染），**必须在后台运行**，避免被 Agent 的超时限制中断。

**规则：**

1. 进入 remotion 项目根目录
2. 使用后台进程启动渲染，并将输出重定向到日志文件
3. 立即返回控制权，定期检查渲染进度
4. 渲染完成后自动进入质检环节

**跨平台后台运行方式：**

| 平台 | 后台运行命令 | 日志文件 |
|---|---|---|
| macOS / Linux | `nohup node scripts/case-publish.mjs <slug> > render-<slug>.log 2>&1 &` | `render-<slug>.log` |
| Windows | `start /B node scripts/case-publish.mjs <slug> > render-<slug>.log 2>&1` | `render-<slug>.log` |

**后台渲染流程：**

```bash
# 进入 remotion 项目目录
cd <remotion-project-path>

# 启动后台渲染（所有平台通用写法）
# 将输出写入日志文件，进程在后台运行，不受超时限制
node scripts/case-publish.mjs <slug> > render-<slug>.log 2>&1 &
echo "渲染进程已启动，PID: $!"
```

**进度检测方式：**

```bash
# 方式 1：检查进程是否仍在运行
# macOS/Linux:
ps aux | grep "case-publish.mjs <slug>" | grep -v grep
# Windows:
tasklist | findstr node

# 方式 2：查看日志文件最新内容
tail -5 render-<slug>.log

# 方式 3：检查输出文件是否已生成
ls -la out/<slug>/<slug>.mp4 2>/dev/null && echo "渲染完成" || echo "渲染中..."
```

**推荐做法（Agent 循环检测 + 完成通知）：**

```
1. 启动后台渲染进程
2. 告知用户"渲染已启动，预计需要 X 分钟，请稍候，完成后我会通知你"
3. 每 30 秒检测一次：
   a. 检查 render-<slug>.log 是否有新进度输出
   b. 检查 out/<slug>/<slug>.mp4 是否已生成
4. 检测到文件生成后，进入质检环节
5. 如果超过 30 分钟仍未完成，检查日志是否有错误

**关键：渲染完成后，必须主动通知用户。**
检测到渲染完成时，立即向用户发送一条消息，例如：
  "✅ 视频渲染完成！正在做最后的质检，请稍等……"
质检完成后，再次通知用户：
  "🎬 你的视频已经生成好了！"
  然后通过 present_files 将 mp4 和封面图展示给用户。
```

**补充说明：**
- `case-publish.mjs` 内部会依次执行：解析分镜稿 → 下载 B-roll → 生成配音 → 渲染 → 抽封面，每步都有控制台输出，日志中可追踪进度
- 渲染完成后，日志最后一行会显示"完成 ✅"
- 支持参数：`--no-tts`、`--no-broll`、`--no-cover`、`--frame=N`

### 第 10 步：质检 + 交付

- 检查 `out/<slug>/<slug>.mp4` 存在，确认大小
- 抽 2-3 帧静帧核对：无内容被裁、字幕不交叠、配色符合题材
- 用 present_files 把 mp4 与封面交付给用户，附一句总时长与题材说明

---

## 安装说明

### 写稿（零依赖，装 Skill 即用）

本 Skill 的**阶段 1（写稿）** 不依赖任何外部项目，纯 Agent 驱动，装好 Skill 就能用：
- 不需要安装 Node.js
- 不需要部署 Remotion
- 不需要申请 API Key
- 不需要 pip install

### 全流程出片（自动部署 Remotion 项目）

如果要用**阶段 2（出片）**，不需要用户手动操作，Agent 会自动完成：

1. 检测 git 环境 → 无则自动安装
2. `git clone https://github.com/scarecrowang/remotion-video-publisher.git`
3. `npm run setup` 自动部署
4. 配置 TTS 和 Pexels（可选，默认免费 TTS 零配置）

#### TTS 配音方案（三选一）

| 方案 | 平台 | 配置 | 音质 |
|---|---|---|---|
| **A. 自动免费 TTS（推荐）** | macOS / Windows / Linux | **零配置**，默认自动按平台选择 | 机器感，可接受，不需 API Key |
| B. 火山引擎（真人感） | 全平台 | 需 `VOLCANO_API_KEY` + `VOLCANO_SPEAKER` | 真人感强，中文最佳 |
| C. ElevenLabs（备选） | 全平台 | 需 `ELEVENLABS_API_KEY` + `ELEVENLABS_VOICE_ID` | 真人感，多语言 |

**方案 A 各平台免费 TTS 详情：**

| 平台 | 引擎 | 零配置？ | 需安装？ |
|---|---|---|---|
| macOS | `say` 命令 | ✅ 零配置，直接可用 | 系统自带 |
| Windows | PowerShell `System.Speech` | ✅ 零配置，直接可用 | 系统自带 |
| Linux | `espeak-ng` | ❌ 需 `sudo apt install espeak-ng` | 但只需安装一次 |

> 默认 TTS_PROVIDER 设为 `auto`，脚本会自动检测平台并选择对应的免费 TTS。
> 想用真人感配音，在 `.env` 中填入 `TTS_PROVIDER=volcano` 及相关 Key 即可。

#### B-roll 素材配置

Pexels API Key（免费注册，用于 B-roll 素材下载）：
- 注册：https://www.pexels.com/api/
- 在 remotion 项目的 `.env` 中填入 `PEXELS_API_KEY = 你的 key`
- **注意：** 如果未配置 Pexels Key，出片时会自动加 `--no-broll` 参数跳过 B-roll，只渲染纯画面 + 配音 + 字幕，不影响出片。

> **以上所有步骤 Agent 会自动完成，用户只需在对话中确认即可。**

---

## 关键约束

- **纯 Agent 驱动（写稿阶段）**：零 Python 脚本、零 pip 依赖、零安装步骤
- **用户可控制出片时机**：说"自动出片" → 全自动；说"写稿" → 先出稿，确认后再出片
- **字数控制**：案例型 1150–1200 字，科普型 1000–1200 字，观点型/教程型 900–1100 字
- **信源可追溯**：每条数据标注出处，单一信源明示风险
- **出片阶段自动部署**：阶段 2 自动检测 git → clone remotion 项目 → npm run setup，用户零手动操作
- **Pexels Key 可选**：未配置时自动加 `--no-broll` 跳过 B-roll，不影响出片

---

## 定时任务模式：每日一期

用户说"每天定时跑一期"、"设置每日视频"、"每天自动出一期"、"定时任务"时，进入此模式。

### 概述

定时任务模式支持**两种触发方式**，共用同一套去重机制和选题储备池：

| 触发方式 | 适用场景 | 示例 |
|---|---|---|
| **A. Agent 自身自动化系统** | WorkBuddy、Claude Code、Codex 等内置了自动化/定时任务功能的 Agent | 用户说"每天早8点出一期" → Agent 调用自身自动化 API 创建定时任务 |
| **B. 自然语言描述** | 任何 Agent 平台，包括没有自动化系统的环境 | 用户说"帮我每天出一期心理疏导视频" → Agent 在对话中主动按日执行 |

> **两种方式共享同一个 `scripts/.publish-history.json` 和 `scripts/topic-pool.txt`，确保历史记录和去重判断全局一致。**

---

### 方式 A：利用 Agent 自身自动化系统

用户只需告知 Agent 想设置定时任务，Agent 自动创建：

**用户说：** "每天早 8 点自动出一期视频"
**Agent 执行：**
1. 确认用户希望的内容类型（如"案例型"）
2. 确认赛道/风格偏好（如"心理疏导"）
3. 创建自动化定时任务：
   - 名称：`video-publisher-每日一期`
   - 频率：每天，用户指定的时间
   - 执行 prompt：`用 video-publisher 生成一期视频，内容类型：案例型，赛道：心理疏导，自动出片。`
4. 告知用户定时任务已生效

**用户说：** "查看定时任务" → Agent 列出当前自动化列表
**用户说：** "取消定时任务" → Agent 删除对应自动化

**适配不同 Agent 平台：**

| Agent 平台 | 自动化机制 | 适配方式 |
|---|---|---|
| **WorkBuddy** | 内置 Automation 系统 | Agent 调用 automation_update 工具创建/查看/删除 |
| **Claude Code** | 无内置自动化，依赖 crontab / 脚本 | Agent 引导用户设置 crontab，或生成脚本 |
| **Codex** | 无内置自动化，依赖 crontab / 脚本 | 同上 |
| **其他 Agent** | 视平台能力 | Agent 优先使用平台自有自动化能力，无则降级到方式 B |

---

### 方式 B：自然语言描述（所有 Agent 通用）

如果 Agent 平台没有自动化系统，或用户更习惯自然语言描述，Agent 在对话中主动按需执行：

**用户说：** "帮我每天出一期心理疏导视频，自动出片"
**Agent 执行：**
1. 确认用户的内容类型偏好
2. 确认选题赛道
3. 告知用户："好的，我记住了。从今天起，每次对话开始时我会主动出一期。如果你希望我每天固定时间推送，可以告诉我你的 Agent 平台（WorkBuddy/Claude Code/Codex），我可以帮你设置自动化定时任务。"

**用户说：** "今天再出一期"
**Agent 执行：**
1. 读取历史记录，执行去重判断
2. 从储备池选一个不重复的选题
3. 写稿 → 出片
4. 更新历史记录

**用户说：** "这几期感觉有点重复，换换方向"
**Agent 执行：**
1. 展示近 N 期的选题列表
2. 询问用户想调整的方向
3. 补充新选题到储备池
4. 从新方向选一个不重复的选题执行

---

### 选题储备池（Topic Pool）

定时任务模式下，选题从储备池中选取（而非用户临时输入），确保内容可控、可计划。

**案例型储备池（示例）：** 以下为**心理疏导赛道**示例（用户可根据自身赛道替换）：

```
心理疏导｜婚前焦虑｜"婚前焦虑"深度拆解：从心理学看那些"恐婚"的人到底在怕什么？｜案例型
心理疏导｜相亲沟通｜相亲中"不会聊天"的底层逻辑，学会这3句话就够了｜教程型
心理疏导｜择偶标准｜"我要求不高，但就是找不到"——择偶标准里的认知陷阱｜科普型
心理疏导｜分手后遗症｜分手后为什么走不出来？神经科学告诉你答案｜科普型
心理疏导｜自我成长｜为什么你越"讨好"对方越不珍惜？｜观点型
心理疏导｜社交恐惧｜社恐的人怎么脱单？这3个方法亲测有效｜教程型
心理疏导｜原生家庭｜原生家庭如何影响你的择偶观？｜科普型
心理疏导｜吸引力法则｜吸引力法则在恋爱中真的有用吗？｜观点型
心理疏导｜父母催婚｜被催婚的人，到底在焦虑什么？｜案例型
心理疏导｜亲密关系｜亲密关系里的"安全型依恋"是什么样的？｜科普型
```

> **用户可自定义储备池：** 在 `scripts/topic-pool.txt` 中按行维护自己的选题，每行格式：`赛道｜关键词｜完整选题描述｜内容类型`。
> 如果该文件不存在，Agent 在首次运行定时任务时，根据用户赛道自动生成第一批储备池。

**自动补充机制：** 当储备池中剩余选题 ≤ 3 个时，Agent 自动执行：
1. 根据历史记录已有选题的赛道/方向，搜索全网最新热点
2. 生成 5-10 个新选题补充到储备池
3. 告知用户已补充哪些新选题

---

### 每日运行流程（定时任务）

```
[定时任务触发 / 用户说"再出一期"]
    │
    ▼
(1) 读取 scripts/.publish-history.json → 获取历史记录
    │
    ▼
(2) 读取 scripts/topic-pool.txt → 获取储备池
    │
    ▼
(3) 遍历储备池，对每个候选选题执行去重判断
    │  ├─ 不重复 → 选中该选题
    │  └─ 重复 → 跳过，选下一个
    │
    ▼
(4) 如果所有候选选题都重复：
    │   ├─ 启动自动补充机制（搜索+生成新选题）
    │   └─ 用新选题重试去重判断
    │
    ▼
(5) 选中选题 → 进入阶段 1 写稿流程（自动出片模式）
    │
    ▼
(6) 写稿完成 → 自动进入阶段 2 出片流程
    │
    ▼
(7) 渲染完成后 → 更新历史记录文件
    │   ├─ 追加新 entry 到 .publish-history.json
    │   ├─ 从储备池中移除已使用的选题
    │   └─ 检查储备池剩余数量，≤3 则自动补充
    │
    ▼
(8) 通知用户：
    ├─ "🎬 每日一期已完成！今天生成的是：《XXX》"
    ├─ "📅 已更新历史记录，储备池剩余 X 个选题"
    └─ 通过 present_files 交付视频和封面
```

---

### 关键规则

- **去重是全局硬性规则**：无论定时任务还是手动输入，都必须执行去重判断，不可跳过
- **定时任务默认自动出片**：定时触发时不需要用户确认，写稿后直接渲染
- **储备池优先**：定时任务模式下不从用户临时输入获取 topic，仅从储备池选取
- **自动补充保底**：储备池耗尽时自动搜索补充，不影响连续运行
- **历史可追溯**：用户可随时查看 `.publish-history.json` 了解过去所有产出
- **手动干预**：用户可随时插入新选题到储备池，下一期自动去重后使用

---

### 用户操作示例

**场景 1：首次设置定时任务**
```
用户：每天早 8 点自动出一期视频，赛道是心理疏导，案例型
Agent：好的，已为你创建每日定时任务（08:00）。
       - 储备池已初始化（10 个心理疏导选题）
       - 明天 08:00 将自动生成第 1 期
       - 历史记录已就绪，去重机制已生效
```

**场景 2：用户手动输入 topic（去重拦截）**
```
用户：写一期婚前焦虑的案例视频
Agent：⚠️ 检测到该 topic 与历史记录中（2026-09-04）的《婚前焦虑深度拆解》存在雷同，
       建议换个角度，例如：
       - 聚焦"婚前焦虑中的原生家庭因素"
       - 改为教程型："3 个方法缓解婚前焦虑"
       或者我在储备池中为你选一个不重复的选题？
```

**场景 3：日常定时运行**
```
[每天早上 08:00 自动触发]
Agent：🎬 每日一期已启动！
       今天从储备池中选中的选题：《相亲中"不会聊天"的底层逻辑》
       经去重检查，确认无重复，开始写稿…
[写稿完成]
Agent：✍️ 口播稿已完成，自动进入渲染阶段…
[渲染完成]
Agent：🎬 每日一期已完成！今天生成的是：《相亲中"不会聊天"的底层逻辑》
       📅 已更新历史记录，储备池剩余 9 个选题
```

**场景 4：储备池不足**
```
Agent：⚠️ 储备池仅剩 2 个选题，正在自动补充新选题…
       ✅ 已补充 6 个新选题到储备池，明天正常运行
```

---

## 输出示例

### 仅写稿
```
scripts/
├── self-driving-grid-视频文稿.md    ← 口播稿（待确认）
```

### 全流程出片
```
scripts/
├── self-driving-grid-视频文稿.md    ← 口播稿

remotion-video-publisher/
├── content/self-driving-grid.md    ← 分镜稿
└── out/self-driving-grid/
    ├── self-driving-grid.mp4       ← 视频
    └── self-driving-grid-thumb.png ← 封面
```

## 参考文件

- `references/script-template.md` — 口播稿模板（5 种类型，字数控制，时长估算）
- `references/md-grammar.md` — 分镜稿 md 语法（head/points/data/stats/theory/out 卡型）
- `references/type-templates.md` — 视频题材模板（counseling/energy/warm 的 palette、B-roll 基调、常用卡序列）

## 本 Skill 源码

- GitHub 仓库：[scarecrowang/video-publisher](https://github.com/scarecrowang/video-publisher)